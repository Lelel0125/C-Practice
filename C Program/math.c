#include <stdio.h>
#include <math.h>

int main(){
    
    float x = 1.57;

    // x = sqrt(x); //平方根
    // x = pow(x, 4);  //次方
    // x = round(x);  //四捨五入
    // x = ceil(x);  //取比大的最近整數
    // x = floor(x);  //取比小的最近整數
    // x = abs(x);  //絕對值
    // x = log(x);  //自然常數log

    //三角函數 in rads 1 = 57.3°
    // x = sin(x);
    // x = cos(x);
    // x = tan(x);

    printf("%f", x);

    return 0;
}